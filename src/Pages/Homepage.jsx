import React from 'react';

const Homepage = () => {
    return (
        <section>
            <h2>Welcome!</h2>

            <button>Login</button>
            <button>Sign up</button>

        </section>
    );
};

export default Homepage;